# 怎麼使用 YEE 式機器龍的指令?

import Adsense from "@components/mdx/Adsense";

1. 當要呼叫 YEE 式機器龍的指令的時候使用 `&` 或是 `@YEE式機器龍` 開頭
2. 在 `&` 之後加上 `指令名稱`
3. 如有額外 `子指令` 或 `參數` 需要傳遞，使用 `空格` 分割
4. `<>` 內為必填， `[]` 內為非必填參數

以下是使用的範例

![使用指令範例](@static/docs/commands/example-usage.png)

<Adsense />

## 我忘記怎麼使用的時候怎麼辦

別擔心！你可以在每個指令後頭加上 `help`，我就會告訴你怎麼用了！

![指令幫助清單範例](@static/docs/commands/example-help.png)

<Adsense />
